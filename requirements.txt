Django==5.2.14
ruff==0.15.11
pre-commit==4.5.1
mysqlclient>=2.2
celery[redis]>=5.3
networkx>=3.3
numpy<1.26
django-vite==3.1.0
gunicorn>=21.2
pandas>=2.1
pyarrow>=15.0
# MCP server (hippie_mcp). Brings starlette, uvicorn, pydantic and httpx2 (the
# package really is named httpx2 — it is what starlette's TestClient uses here,
# not httpx) with it.
# Upper bound is deliberate: this SDK removed its whole v1 surface (FastMCP,
# mcp.server.fastmcp) in v2, and hippie_mcp imports v2-only symbols with no shim.
mcp>=2.0,<3.0
